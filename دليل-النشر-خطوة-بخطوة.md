# دليل النشر خطوة بخطوة — معمل الحوشي للتحاليل الطبية
## المزوّد السحابي: **Supabase** (قاعدة بيانات PostgreSQL مجانية)

الهدف: الموقع يبقى أونلاين على رابط ثابت، وكل البيانات (الحجوزات، المرضى، الروشتات، النتائج) تتخزن في قاعدة بيانات سحابية وتظهر على كل الأجهزة فوراً، ولما تعدّل أي كود على جهازك يتحدث الموقع أوتوماتيك.

**الترتيب:** Supabase ← GitHub Desktop ← Vercel ← تفعيل المزامنة من لوحة المدير.

> ⏱ المدة الكلية: حوالي ١٥ دقيقة مرة واحدة. وبعد كده أي تعديل = دقيقتين (Commit + Push).

---

## الخطوة 1 — إنشاء قاعدة البيانات على Supabase (٥ دقائق)

1. افتح **supabase.com** واضغط **Start your project** وسجّل بحساب جيميل.
2. اضغط **New project** واملأ:
   - **Name:** `elhoshy-lab`
   - **Database Password:** كلمة سر قوية — **اكتبها عندك في ورقة أو الملاحظات** (مهمة للدعم الفني).
   - **Region:** **`Frankfurt (eu-central-1)`** — الأقرب لمصر.
   - اضغط **Create new project** واستنى دقيقتين لحد ما يجهز.
3. من القائمة اضغط **SQL Editor → New query** — الصق محتوى الملف **`supabase/schema.sql`** الموجود مع المشروع كامل — واضغط **Run**.
   - لو ظهر **Success. No rows returned** يبقى تمام.
   - الملف بينشئ جدول `lab_data` والسياسات (RLS) ومجلد الملفات.
5. اضغط **Storage** من القائمة — **New bucket** → الاسم `lab-files` → فعّل **Public bucket** → **Create**.
6. تأكيد: **Table Editor** → هتلاقي جدول اسمه **`lab_data`** فاضي ✅
7. 📄 **دلوقتي افتح الملف `supabase/ازاي-تجيب-المفاتيح.md`** — فيه شرح بالصور والكلام إزاي تجيب **Project URL** و **API key** بالتفصيل الممل مع شكل كل واحد.

> **نوعين المفاتيح والاتنين شغالين:** `sb_publishable_...` (الجديد) أو `eyJhbGciOi...` (القديم anon).
> ⛔ **ممنوع تنسخ** أي مفتاح اسمه `sb_secret_...` أو `service_role`.

**شكل النتيجة النهائية اللي هتطلع بيها:**
```
Project URL        : https://qwrtyxmnbvcxzasdfghj.supabase.co
anon / publishable : sb_publishable_9Xk2LmQ7vTzR4nB8cFdGhJ_2vQ8rTnK6wYzLpXcQaZs
مجلد الصور         : lab-files
```

> 💡 **الخطة المجانية:** 500 ميجا قاعدة بيانات + 5 جيجا نقل + 1 جيجا ملفات — **ومفيش حد لعدد القراءات**. معمل زي ده مش هيوصل لجزء بسيط من الحد ده.

---

## الخطوة 2 — رفع الكود على GitHub بـ GitHub Desktop (٥ دقائق)

1. لو مش عندك حساب: افتح **github.com** → **Sign up** → اعمل حساب (بريد + كلمة سر).
2. حمّل **GitHub Desktop** من **desktop.github.com** وثبّته وافتحه.
3. **Sign in to GitHub.com** → سجّل بنفس الحساب → **Finish**.
4. من القائمة **File → Add local repository** → **Choose…** → اختار فولدر **`elhoshy-lab`** (بعد ما فكّيت الضغط عن الملف المضغوط).
5. لو ظهرت رسالة *“this directory does not appear to be a Git repository”* → اضغط **create a repository**:
   - **Name:** `elhoshy-lab`
   - سيب باقي الخيارات زي ما هي → **Create Repository**.
6. كل الملفات هتظهر في تبويب **Changes**. في خانة **Summary** (تحت على الشمال) اكتب: `أول نسخة من موقع المعمل` → اضغط **Commit to main**.
7. اضغط **Publish repository** (الزر الأزرق فوق):
   - **Name:** `elhoshy-lab`
   - **Public** (أسهل مع Vercel المجاني) — أو Private وبيشتغل برضه.
   - اضغط **Publish Repository**.
8. الكود دلوقتي على `github.com/اسمك/elhoshy-lab` ✅

---

## الخطوة 3 — الاستضافة على Vercel (٣ دقائق)

1. افتح **vercel.com** → **Sign Up** → اختار **Continue with GitHub** → وافق على الصلاحيات.
2. من لوحة Vercel اضغط **Add New → Project**.
3. قدام `elhoshy-lab` اضغط **Import**.
4. في صفحة الإعدادات:
   - **Framework Preset:** سيبه **Other**
   - **Build Command و Output Directory:** **فاضيين** (الموقع ثابت — مفيش بناء)
   - **Root Directory:** `./`
   - اضغط **Deploy**.
5. استنى 30–60 ثانية → هيظهر **Congratulations** ورابط زي `https://elhoshy-lab.vercel.app` → اضغط **Visit**.
6. **(اختياري) دومين خاص:** من صفحة المشروع → **Domains** → اكتب الدومين اللي اشتريته واتبع التعليمات.

---

## الخطوة 3 bis) — متغيرات البيئة (مهمة للأمان) 🆕

من صفحة المشروع على Vercel: **Settings ▸ Environment Variables** ← أضف التلاتة دول:

| الاسم | القيمة | منين |
|---|---|---|
| `SUPABASE_URL` | `https://xxxx.supabase.co` | Supabase ▸ Settings ▸ API |
| `SUPABASE_SERVICE_KEY` | المفتاح **السرّي** (Secret / service_role) | Supabase ▸ Settings ▸ API |
| `APP_SECRET` | كلمة عشوائية طويلة | اكتبها أنت |

ثم **Deployments ▸ ⋯ ▸ Redeploy** (مرة واحدة).

> دي المتغيرات الوحيدة اللي الموقع محتاجها — وبتُقرأ على السيرفر بس، **مش بتوصل المتصفح أبداً**.

## الخطوة 4 — تفعيل المزامنة السحابية (دقيقة)

1. افتح `رابط-موقعك/admin.html`
2. سجّل الدخول: **اسم المستخدم `admin`** — **كلمة المرور `Mo@010`**
   > ⚠ غيّر كلمة السر من **الدكاترة → تعديل** بعد أول دخول.
3. من القائمة الجانبية اضغط **الإعدادات**، وانزل لآخر الصفحة لكارت **الربط السحابي**:
   - **مزوّد الخدمة:** Supabase (ثابت)
   - **Project URL:** الصق اللي نسخته في الخطوة 1
   - **anon public key:** الصق المفتاح
   - **مجلد الملفات (Bucket):** `lab-files`
   - **طريقة الاتصال:** **عبر السيرفر الآمن /api** ⭐ (الأأمن — المتصفح مش بيشوف أي مفتاح)
   - **المزامنة:** مفعّلة
   - اضغط **حفظ واختبار الاتصال**
   - **سجّل خروج ودخول تاني** علشان تاخد توكن صالح.
4. لو ظهرت رسالة **«نجح الاتصال — السحاب جاهز»** يبقى تمام ✅
   - أول مرة هيرفع بيانات المعمل (التحاليل والأسعار والدكاترة) للسحاب — يستنى ثواني.
   - في ركن الشاشة تحت على الشمال هتلاقي شارة **«متصل بالسحاب»** باللون الأخضر.
   - كرّر نفس الخطوة على أي جهاز تاني تستخدمه (بنفس المفاتيح).

**من اللحظة دي:**
- أي مريض يحجز من موبايله → الحجز يوصل للسحاب → **يظهر فوراً في لوحة المدير على أي جهاز**.
- أي تعديل في الأسعار أو إضافة تحليل من اللوحة → يتوزع على كل الزوار.
- لو الإنترنت مقطوع: الشارة بتبقى **«تعذر الاتصال»** والموقع يكمل شغال بالبيانات المحلية، وأول ما الإنترنت يرجع يزامن تلقائي.

---

## الخطوة 5 — طريقة تحديث الموقع بعد كده (الروتين اليومي)

1. عدّل الملفات اللي عايزها على جهازك.
2. افتح **GitHub Desktop** → التعديلات هتظهر في تبويب **Changes**.
3. اكتب وصف التعديل في **Summary** → **Commit to main**.
4. اضغط **Push origin** (فوق).
5. **Vercel بينشر النسخة الجديدة أوتوماتيك خلال 30–60 ثانية** — مفيش حاجة تانية تعملها.
   - تتابع من: صفحة المشروع على Vercel → تبويب **Deployments** → لما يبقى **Ready** بالأخضر يبقى وصل.

---

## بيانات الدخول (احفظها)

| المستخدم | اسم المستخدم | كلمة المرور |
|---|---|---|
| المدير العام (مالك المعمل) | `admin` | `Mo@010` |
| طبيب 1 | `heba` | `123456` |
| طبيب 2 | `kareem` | `123456` |
| المريض | رقم موبايله | آخر ٦ أرقام من الموبايل |

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| شارة المزامنة **«تعذر الاتصال»** | تأكد إن الـ URL **من غير `/` في الآخر**، والـ anon key كامل، وإنك شغّلت ملف `schema.sql` |
| خطأ 404 أو «relation lab_data does not exist» | ارجع لـ **SQL Editor** وشغّل `supabase/schema.sql` تاني |
| صور الروشتات مش بتظهر | تأكد إن bucket اسمه **`lab-files`** و **Public** مفعل (Storage ▸ أيقونة النقاط الثلاث ▸ Make public) |
| البيانات مش بتنتقل بين الأجهزة | فعّل المزامنة في **الإعدادات** على كل جهاز بنفس URL والمفتاح، واضغط «سحب من السحاب» |
| Vercel نجح بس الموقع فاضي | تأكد إن `index.html` في **جذر** الفولدر مش جوه فولدر تاني |
| عايز أمسح كل حاجة وأبدأ من جديد | لوحة المدير ▸ الإعدادات ▸ **إعادة ضبط المصنع** |

---

## النسخ الاحتياطي (مهم)

من لوحة المدير → **الإعدادات → النسخ الاحتياطي والاستعادة → تصدير نسخة كاملة (JSON)**.
خد نسخة كل أسبوع — دي نسخة كاملة من كل الحجوزات والمرضى والنتائج والأسعار، وتقدر ترجّعها بـ «استيراد نسخة» في أي وقت.

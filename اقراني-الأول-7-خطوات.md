# اقراني الأول — 7 خطوات وكل حاجة هتبان على الموقع

## ليه مفيش حاجة اتغيرت عندك؟

التعديلات كلها كانت موجودة في نسخة التطوير (عندي)، لكن **مستودع GitHub بتاعك لسه قديم**،
و**Vercel بيبني الموقع من GitHub** — فطبعاً بينشر النسخة القديمة.

يعني: الكود سليم 100%، لكنه **مش واصل للسيرفر**. الحل بسيط: انسخ الملفات دي عندك واعمل **Push**.

> أهم سطر في الملف ده: **Commit لوحده مش كافي — لازم Push origin.**

---

## الخطوات (بالترتيب)

### 1) فك الضغط
فك الضغط عن ملف `elhoshy-lab.zip` في أي مكان عندك (مثلاً على سطح المكتب).

### 2) افتح فولدر المشروع
GitHub Desktop → اختار مستودع المعمل → **Repository → Show in Explorer**
(على ماك: **Repository → Show in Finder**).

### 3) استبدل الملفات
- احذف **كل** الملفات والفولدرات القديمة اللي في فولدر المشروع،
- **ما عدا فولدر `.git`**（مخفي — متقربوش منه ولا تمسحوه، ده تاريخ المستودع）.
- بعد كده انسخ **كل** محتويات الفولدر اللي فكيت الضغط عنه إلى فولدر المشروع
  (لازم تنسخ فولدر `api` وفولدر `assets` وملف `sw.js` وكل ملفات `.html` و `.md`).

### 4) Commit
ارجع لـ GitHub Desktop → هيلاقي كل التغييرات في الشمال →
- خانة **Summary** اكتب: `تحديث شامل: إصلاحات + سيرفر آمن`
- اضغط **Commit to main**.

### 5) Push ← **أهم خطوة**
فوق يمين الشاشة اضغط **Push origin**.
لو الزرار مش ظاهر يبقى في انتظارك تغييرات متعملهاش commit.
**من غير الخطوة دي مفيش أي حاجة هتتغير على الموقع.**

### 6) استنى Deployment جديد
افتح https://vercel.com/dashboard → مشروع `al-hawashy-laboratory` →
هتلاقي deployment جديد حالة **Building** → استنى لحد ما يبقى **Ready** (غالباً أقل من دقيقة).

### 7) افتح الموقع بـ hard refresh
افتح https://al-hawashy-laboratory.vercel.app واضغط **Ctrl + F5**
(من الموبايل: امسح كاش المتصفح أو افتح من نافذة التصفح الخاص).

كده هتلاقي:
- «سحب منزلي» من غير «لكل الدقهلية».
- شريط الإعلان بقى فوق ومش مغطي على المحتوى.
- زرّ الوضع النهاري/الليلي شغال، والافتراضي نهاري.
- مفيش أي صفحة بتقفل أو بتعلّق.

---

## إزاي تتأكد إن كل الملفات وصلت فعلًا؟ (3 روابط)

بعد ما الـ Deployment يبقى **Ready**، افتح الرابط ده في المتصفح:

1. `https://al-hawashy-laboratory.vercel.app/api/data`
   - لو ظهر **The page could not be found** → يبقى **مجلد `api` لسه موصلش** (ارفعه تاني).
   - لو ظهر `{"error":"POST فقط"}` → تمام، السيرفر شغال ✅

2. `https://al-hawashy-laboratory.vercel.app/sw.js`
   - أول سطرين لازم يكونوا فيهم **`elhoshy-lab-v2`**. لو لقيت `v1` يبقى الملف قديم.

3. `https://al-hawashy-laboratory.vercel.app/sitemap.xml`
   - كل الروابط لازم تكون **`al-hawashy-laboratory.vercel.app`**. لو لقيت `elhoshy-lab.vercel.app` يبقى الملف قديم.

> أهم نقطة: لازم تنسخ **الفولدرات كلها** مش الملفات اللي بتشوفها بس — بالذات فولدر `api` وفولدر `assets`.

## (اختياري) تفعيل المزامنة السحابية الآمنة

الموقع شغال محلي 100% من غير أي قاعدة بيانات. لو عايز الحجوزات والنتائج
تبان على كل الأجهزة، اعمل الآتي مرة واحدة:

1. **Vercel → المشروع → Settings → Environment Variables** ضيف 3 قيم:

   | الاسم | القيمة |
   |---|---|
   | `SUPABASE_URL` | `https://psvcoytngyaklaypagem.supabase.co` |
   | `SUPABASE_SERVICE_KEY` | المفتاح السرّي من Supabase (اللي بيبدأ بـ `sb_secret_` أو service_role) |
   | `APP_SECRET` | أي كلمة طويلة وعشوائية (مثلاً `Hawashy#2026-Lab-9dK2mQ7x`) |

2. **Deployments → آخر deployment → Redeploy** (علشان المتغيرات تتقرأ).

3. من لوحة الأدمن: **الإعدادات → الربط السحابي** →
   - طريقة الاتصال: **عبر السيرفر الآمن /api — الأأمن (مُوصى به)**
   - المزامنة: مفعّلة
   - **حفظ واختبار الاتصال**

4. **سجّل خروج ودخول تاني** من اللوحة (علشان تاخد توكن صالح).

5. شغّل ملف `supabase/schema.sql` مرة واحدة من **SQL Editor** في Supabase
   (النسخة دي مقفولة افتراضياً — مفيش حد يقدر يقرا البيانات غير عن طريق السيرفر).

التفاصيل الكاملة في: `دليل-النشر-خطوة-بخطوة.md` و `الأمان-والخصوصية.md`.

---

## اللي اتصلح في التحديث ده

| المشكلة | الحالة |
|---|---|
| «سحب منزلي لكل الدقهلية» | اتمسحت — بقى «سحب منزلي» بس |
| شريط الإعلان مغطي على المحتوى | اتظبط (sticky فوق، والمحتوى بينزل تحته) |
| مفيش وضع نهاري | موجود، والافتراضي نهاري، وبيتذكر بعد الريلود |
| الصفحات بتقفل / بتعلّق | اتصلح + Service Worker جديد بيمنع الكاش القديم |
| المزامنة السحابية كانت معطّلة بالكامل | **bug خطير اتصلح** — المزوّد كان بيُقرا باسم غلط فالمزامنة كانت بتقفل نفسها |
| أمان البيانات | طبقة سيرفر `/api` جديدة: المتصفح مبيشوفش أي مفتاح، وكل دور بيشوف اللي يخصّه بس |
| الروابط (sitemap/robots) | بقت على النطاق الصح `al-hawashy-laboratory.vercel.app` |

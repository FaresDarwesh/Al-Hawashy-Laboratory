# الأمان وحماية بيانات المرضى — تقرير صريح وخيارات الترقية

> هذا الملف مهم جداً. اقرأه قبل ما تحط بيانات مرضى حقيقية على الموقع.

---

## 1) الوضع الحالي (بصراحة)

الموقع **موقع ثابت** (Static) — مفيش سيرفر خاص بيه. وده معناه:

| النقطة | المعنى |
|---|---|
| البيانات بتتخزن في | متصفح الجهاز (localStorage) + Supabase (لو المزامنة مفعّلة) |
| الموقع مش بيقدر يخبي أي سر | أي مفتاح يتحط في كود المتصفح = **مفتاح عام** لأي حد يفتح DevTools |
| سياسات Supabase الحالية | `allow select/insert/update: if true` = **أي حد عنده المفتاح يقدر يقرأ ويكتب كل حاجة** |

### 🔴 الخطر بالتحديد
لو حطيت الـ `anon key` جوه ملف `assets/js/supabase-config.js` (علشان المرضى يحجزوا من موبايلاتهم)، يبقى:

```bash
curl "https://xxxx.supabase.co/rest/v1/lab_data?select=*" -H "apikey: المفتاح"
```
…هيرجّع **كل أسماء المرضى وأرقامهم وعناوينهم ونتائج تحاليلهم وروشتاتهم**.

### ✅ الوضع الحالي الآمن (نسبياً)
طالما **مش حاطط المفاتيح في الكود** ومفعّل المزامنة **من جهازك أنت بس** من لوحة الأدمن:
- الزوار **معندهمش أي مفتاح** → مش بيقدروا يوصلوا لقاعدة البيانات خالص ✅
- بيانات المرضى بتوصل من موبايلاتهم؟ **لأ** — محتاجة المفتاح (وده الاختيار الواعي تحت).

---

## 2) الحلول الثلاثة (اختار واحد)

### 🅰 الحل الأأمن — طبقة سيرفر (Serverless Proxy) ⭐ مُوصى به
**الفكرة:** المتصفح **مش بيتكلم مع Supabase خالص**. بيتكلم مع `/api/*` على Vercel، والسيرفر هو اللي بيقرأ قاعدة البيانات بمفتاح سرّي محطوط في Environment Variables.

**المميزات:**
- ✅ بيانات المرضى **مش readable** لأي حد (الجدول مقفول تماماً عن العامة: `deny all`)
- ✅ المريض بيدخل برقم موبايله + آخر ٦ أرقام → السيرفر بيتحقق ويرجّع **بياناته هو بس**
- ✅ المدير بيدخل بيوزر/باسورد → السيرفر بيتحقق ويرجّع الصلاحية المناسبة
- ✅ الحجز من أي موبايل شغال 100%
- ✅ المفتاح السرّي **عمره ما بيوصل المتصفح**

**المتطلبات:**
- مجلد `api/` جوّه المشروع (Vercel بيشغّله تلقائي كـ Serverless Functions)
- متغيرات بيئة في Vercel: `DATABASE_URL` (من Supabase ▸ Connect ▸ **Transaction pooler** بورت **6543**) + `APP_SECRET`
- تعديل `sync.js` ينادي `/api/...` بدل Supabase REST
- تشغيل سياسات جديدة في SQL Editor تقفل الجدول عن العامة

**العيوب:** فيه شوية إعداد منك (نسخ `DATABASE_URL` في Vercel)، واستجابة أبطأ بجزء من الثانية.

---

### 🅱 الحل الآمن جداً والأسهل — "البيانات الحساسة محلية بس"
**الفكرة:** الموقع أونلاين للأسعار والحجز، بس **مفيش أي بيانات مريض بتتخزن أونلاين خالص**.

- الحجز من الموبايل → بيفتح **واتساب المعمل** برسالة جاهزة فيها كل التفاصيل (أو يُسجَّل محلياً وانت تنقله)
- الروشتة → بتتبعت على واتساب
- كل بيانات المرضى/الحجوزات/النتائج → **على جهاز المعمل أنت بس** (localStorage)، مش بتتزامن مع أي سيرفر
- صفر بيانات حساسة على الإنترنت = **صفر احتمال تسريب**

**المميزات:** أأمن خيار على الإطلاق، مجاني، مفيش أي إعداد.
**العيوب:** المريض مش هيلاقي حجزه أونلاين، ولو مسحت بيانات المتصفح هتضيع (فلازم نسخة احتياطية دورية).

---

### 🅲 الوضع الحالي + تحسينات جزئية (مخاطرة متوسطة)
- سيب المزامنة شغالة من جهازك بس، مع: قفل تلقائي للوحة، نسخ احتياطي مشفر، وتحذير واضح.
- البيانات موجودة على Supabase بمفتاح على جهازك أنت فقط.
- **المشكلة:** لو الجهاز اتسرق أو حد فتح DevTools عليه → يقدر يقرأ كل البيانات. ومفيش حجز من الموبايلات.

---

## 2 bis) ✅ الوضع الآمن اتبنى بالفعل — خطوات التفعيل (5 دقائق)

الكود فيه دلوقتي مجلد **`api/`** (سيرفر آمن على Vercel) + وضع «عبر السيرفر الآمن» في إعدادات الموقع.

### الخطوة 1: شغّل ملف `supabase/schema.sql` الجديد في SQL Editor
ده بيقفل الجدول عن العامة تماماً (`select using (false)`) — يعني مفيش حد يقدر يقرأ أي حاجة بالمفتاح العام.

### الخطوة 2: حط ٣ متغيرات في Vercel
من لوحة المشروع على Vercel: **Settings ▸ Environment Variables** ← أضف:

| الاسم | القيمة | منين |
|---|---|---|
| `SUPABASE_URL` | `https://psvcoytngyaklaypagem.supabase.co` | Supabase ▸ Settings ▸ API ▸ Project URL |
| `SUPABASE_SERVICE_KEY` | المفتاح **السرّي** (`sb_secret_...` أو `service_role`) | Supabase ▸ Settings ▸ API ▸ **Secret key** |
| `APP_SECRET` | أي كلمة طويلة عشوائية (مثال: `Hw8#kL2$pZ9qR4vT7nM3x`) | اكتبها أنت أو ولّدها |

> ⚠️ متغيرات البيئة دي هي الوحيدة اللي ليها لازمة — وبتتقرأ على السيرفر وبس، **عمرها ما بتوصل المتصفح**.
> بعد الإضافة لازم **Redeploy** (Deployments ▸ ⋯ ▸ Redeploy).

### الخطوة 3: فعّل الوضع من اللوحة
`admin.html` ▸ **الإعدادات** ▸ الربط السحابي ▸ **طريقة الاتصال** = **«عبر السيرفر الآمن /api»** ▸ المزامنة مفعّلة ▸ **حفظ واختيار الاتصال**.

### الخطوة 4: سجّل خروج ودخول تاني
علشان تاخد **توكن** صالح (بيتحقق منه السيرفر). ✅

---

### 🧪 اختبارات الأمان اللي اتعملت على الكود (23/23 نجح)
- زائر: ميقدرش يشوف حجوزات ولا مرضى ولا مصروفات ✅
- زائر: الأطباء بتبان **بدون كلمات مرور** ✅
- زائر: الإعدادات بتبان **بدون مفاتيح السحاب** ✅
- توكن مزوّر أو منتهي → يتعامل كزائر ✅
- زائر: يقدر يضيف حجز (بس الحالة بتتفرض `pending` والسعر بتقفله) ✅
- زائر: **ممنوع** يكتب مصروفات / يعدّل مريض موجود ✅
- مريض: يشوف **بياناته هو بس**، وممنوع يلمس حجز غيره ✅
- طبيب: ممنوع يكتب مصروفات ✅
- أدمن: صلاحيات كاملة ✅

---

## 3) إجراءات احترازية اتعملت فعلاً في الكود ✅

1. **قفل تلقائي للوحة** (الإعدادات ▸ الأمان) — بيقفل الجلسة بعد 20 دقيقة عدم استخدام (تقدر تعدّلها 5/10/20/30/60 أو تعطّلها).
2. **تحذير أمني أحمر** جوه كارت الربط السحابي.
3. **كارت إعدادات الأمان** في الإعدادات (قفل تلقائي + قواعد مهمة).
4. **صفحة سياسة خصوصية** (`privacy.html`) مرتبطة في الفوتر.
5. المفاتيح **مش** موجودة في أي ملف كود (فاضية في `supabase-config.js`).
6. الموقع كله على **HTTPS إجباري** + هيدرات أمان في `vercel.json`.

---

## 4) سياسات Supabase المُوصى بيها (حسب اختيارك)

### لو اخترت 🅑 أو 🅒 (مفيش حجز من الموبايلات) — شغّل ده في SQL Editor:
```sql
-- قفل كل القراءة والكتابة عن العامة (البيانات على جهازك أنت بس)
drop policy if exists "lab_read"   on public.lab_data;
drop policy if exists "lab_insert" on public.lab_data;
drop policy if exists "lab_update" on public.lab_data;

create policy "lab_none" on public.lab_data for select using (false);
create policy "lab_noins" on public.lab_data for insert with check (false);
create policy "lab_noupd" on public.lab_data for update using (false);
```
> بعد كده المزامنة هتبان «تعذر الاتصال» — وده **المطلوب**: مفيش بيانات حساسة بتطلع بره جهازك.

### لو اخترت 🅐 (طبقة السيرفر) — نفس السياسات فوق + السيرفر بيتصل بـ service_role.

### سياسات مؤقتة لو لازم حجز من الموبايلات (بدون سيرفر):
```sql
-- العامة تقدر تضيف حجز/روشتة بس، ومتقرأش أي حاجة
create policy "pub_insert" on public.lab_data
  for insert with check (tbl in ('bookings','prescriptions'));
create policy "no_read" on public.lab_data for select using (false);
```
> ⚠️ لكن كدة **الأدمن نفسه مش هيقدر يقرا الحجوزات من جهاز تاني** — محتاج طبقة السيرفر.

---

## 5) التزام قانوني (مهم)

بيانات التحاليل الطبية تعتبر **بيانات شخصية حساسة** بموجب **قانون حماية البيانات الشخصية المصري رقم 151 لسنة 2020** — واللي بيشترط:
- موافقة المريض على جمع بياناته
- تأمينها ضد الوصول غير المصرح
- إتاحة حذفها عند الطلب (موجود: شاشة المرضى ▸ حذف)

---

## 6) نصايح عملية ليك شخصياً

- 🖥 افتح لوحة الأدمن **على كمبيوتر المعمل بس**، مش على موبايلك الشخصي.
- 🔒 سيب القفل التلقائي مفعّل (10–20 دقيقة).
- 💾 خد **نسخة احتياطية أسبوعياً** (الإعدادات ▸ تصدير نسخة كاملة) وخزّنها في فلاشة أو جوجل درايف مشفر.
- 🚫 عمرك ما تبعت المفتاح لأي حد — ولا المبرمج اللي هيتعامل مع الموقع بعد كده (اديله مفتاح مشروع تجريبي).
- 👤 امسح بيانات المرضى اللي انتهت الحاجة ليها دورياً.

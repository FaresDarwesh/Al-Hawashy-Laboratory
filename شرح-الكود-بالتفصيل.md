# شرح الكود بالتفصيل — معمل الحوشي للتحاليل الطبية
### من الألف إلى الياء: كل ملف، كل دالة، كل جدول، وبروتوكول المزامنة

---

## 1) نظرة عامة على المعمارية

الموقع **Static Site خالص**: HTML + CSS + JavaScript صرف — **بدون أي مكتبة خارجية، بدون React، بدون jQuery، بدون CDN، بدون Node.js على السيرفر**. الرسوم البيانية و QR Code ومحرّك المزامنة كلهم مكتوبين من الصفر داخل المشروع.

```
المتصفح
 ├─ core.js      ← قاعدة البيانات (localStorage) + المحرك العام (toast/modal/icon/QR)
 ├─ shared.js    ← مساعدات مشتركة + سلة التحاليل (Cart)
 ├─ sync.js      ← المزامنة السحابية مع Supabase عبر REST
 ├─ qr.js        ← مولّد QR Code من الصفر
 ├─ charts.js    ← راسم الرسوم البيانية من الصفر (Canvas)
 └─ صفحات: app.js / booking.js / patient.js / admin.js / admin2.js / doctor.js

السحاب (اختياري): Supabase (جدول lab_data + مجلد lab-files للصور)
```

**قاعدة البيانات محلياً:** كائن JSON واحد محفوظ في `localStorage` تحت المفتاح `elhoshy_lab_db_v1`.
**الجلسة:** `elhoshy_lab_session_v1` (admin / doctor / patient).
**حالة المزامنة:** `elhoshy_cloud_state_v1`.

---

## 2) هيكل الملفات

```
elhoshy-lab/
├─ index.html            الصفحة الرئيسية (هيرو، خدمات، تحاليل، باقات، خطوات، فروع، دكاترة، آراء، FAQ)
├─ tests.html            كتالوج التحاليل والباقات مع بحث وفلترة
├─ booking.html          رحلة الحجز (4 خطوات)
├─ prescription.html     رفع الروشتة + التحليل الذكي
├─ track.html            تتبع الحجز بالكود والموبايل
├─ patient.html          الملف الطبي للمريض
├─ contact.html          تواصل معنا
├─ admin.html            لوحة تحكم المدير (16 شاشة)
├─ doctor.html           بوابة الطبيب (9 شاشات)
├─ 404.html              صفحة الخطأ
│
├─ assets/css/style.css  نظام التصميم كامل (داكن فقط)
├─ assets/js/
│   ├─ core.js           (800 سطر) النواة + قاعدة البيانات + الأيقونات
│   ├─ shared.js         (170) كروت التحاليل + السلة + العروض + الإشعارات
│   ├─ app.js            (115) الصفحة الرئيسية
│   ├─ booking.js        (440) رحلة الحجز
│   ├─ patient.js        (287) الملف الطبي
│   ├─ admin.js          (659) شاشات الأدمن الأساسية + الرسوم
│   ├─ admin2.js         (943) CRUD + الرسائل + الملصق + الإعدادات
│   ├─ doctor.js         (518) بوابة الطبيب
│   ├─ charts.js         (318) راسم الرسوم (Canvas)
│   ├─ qr.js             (293) مولّد QR من الصفر
│   ├─ sync.js           (556) المزامنة السحابية
│   └─ supabase-config.js إعداد Supabase الافتراضي (اختياري)
├─ assets/img/           doctor.jpg / doctor.png
│
├─ manifest.json + sw.js   PWA (تثبيت على الموبايل + عمل بدون إنترنت)
├─ netlify.toml / vercel.json / .htaccess / robots.txt / sitemap.xml
├─ supabase/schema.sql     إنشاء جدول Supabase والسياسات
├─ دليل-النشر-خطوة-بخطوة.md
└─ README.md
```

---

## 3) نموذج البيانات (DB)

الكائن المحفوظ في `localStorage`:

| المفتاح | المحتوى |
|---|---|
| `tests` | ~104 تحليل: `{id, name, cat, price, code, prep, sample, hours, homeExtra, active}` |
| `packages` | 10 باقات: `{id, name, price, oldPrice, tests:[ids], cat, active}` |
| `branches` | فرعين: `{id, name, address, phone, hours, active}` — فرع برج النور الحمص / فرع منشأه الاخوة |
| `areas` | 7 مناطق: `{id, name, fee}` — شنفاس 50، البهو فريك 50، السبخا 60، ديرب 60، بقطارس 70، برج النور الحمص 50، المنشيه 60 |
| `doctors` | `{id, name, title, specialty, phone, username, password, role:'owner'|'doctor', active, commission, homePrices:{areaId:price}, avatar, bio}` |
| `bookings` | `{id, code, patient, phone, whatsapp, branchId, type:'home'|'lab', areaId, address, items[], extras, total, date, time, doctorId, payment:'cash'|'instapay', receipt, paymentStatus, status, timeline[], createdAt}` |
| `patients` | `{id, name, phone, password, gender, age, files[], results[], createdAt}` |
| `prescriptions` | `{id, code, name, phone, images[], tests[], price, status, reply, createdAt}` |
| `expenses` | `{id, title, cat, amount, date, by}` |
| `offers` | `[]` (فاضي — الإدارة تضيف) |
| `reviews` | `{id, name, area, rate, text, active, date}` |
| `faqs` | `{id, q, a}` |
| `notifications` | `{id, title, body, type, to, at, read}` |
| `activity` | سجل العمليات `{id, text, type, at, by}` |
| `counters` | `{booking:0, rx:0, patient:0}` |
| `settings` | إعدادات المعمل (شوف تحت) |

**`settings`:**
```js
{
  labName, labShort, ownerName, ownerTitle, ownerBio,
  phones:['01023290567'], whatsapp:'201023290567',
  instapay:'01010168622', instapayName, email,
  address:'برج النور الحمص', workFrom:'08:00', workTo:'23:00',
  closedDays:[5], homeServiceEnabled:true, homeBaseFee:50, slotMinutes:30,
  about, heroTitle, heroSub,
  cloud:{ provider:'supabase', url, key, bucket, enabled }
}
```

> كل البيانات الوهمية فاضية عن قصد: `bookings=[]`, `patients=[]`, `prescriptions=[]`, `expenses=[]`, `offers=[]`, `counters` من الصفر — جاهز للعيش فوراً.

---

## 4) تفصيل الملفات

### `assets/js/core.js` — النواة
| الدالة | الوظيفة |
|---|---|
| `lsGet/lsSet/lsDel` | طبقة تخزين آمنة — لو المتصفح مانع localStorage بيشتغل في الذاكرة |
| `seed()` | بيانات المصنع (التحاليل، الباقات، الفروع، المناطق، الدكاترة) |
| `migrate(db)` | `SCHEMA=4` — يفرض الفروع والمناطق والباسورد ويمسح أي بيانات قديمة |
| `load() / save() / db() / resetDB()` | إدارة قاعدة البيانات + نسخ احتياطي |
| `exportDB() / importDB()` | تصدير/استيراد ملف JSON |
| `getSession / setSession / logout / currentUser` | الجلسات (admin / doctor / patient) |
| `toast() / modal() / confirm()` | عناصر الواجهة المنبثقة |
| `fileToDataURL() / downloadBlob() / copyText()` | الملفات والتحميل |
| `log() / notify()` | سجل العمليات + الإشعارات |
| `buildShell(active)` | يبني الهيدر + الفوتر + شريط التمرير + شريط تقدم القراءة |
| `initFX()` | حركات الظهور، العدّادات التصاعدية، ripple، **حقن الأيقونات** (`watchIcons`) |
| `initParticles()` | خلفية الجزيئات المتحركة في الهيرو |
| `ICONS / icon(name,size,color) / stars(n)` | 40 أيقونة SVG + نجوم التقييم |
| `hydrateIcons() / watchIcons()` | تحويل `[data-icon]` إلى SVG تلقائياً (MutationObserver) |
| `validatePhone() / required()` | تحقق من المدخلات |
| `uid / $ / $$ / el / money / fmtDate / ago / escapeHtml` | أدوات عامة |

### `assets/js/shared.js`
`cGet/cSet` (سلة آمنة التخزين)، `testCard()`، `packageCard()`، `pkgModal()`، `offersSlider()`، `notifBell()`.
بيانات السلة: `{branchId, type, areaId, items:[{id,name,price,homeExtra,qty}]}`.

### `assets/js/booking.js` — رحلة الحجز
- 4 خطوات: **النوع/المكان → التحاليل → البيانات → الدفع/المراجعة**
- `homeFee()` يحسب رسوم السحب: سعر الطبيب المخصص للمنطقة ← وإلا رسوم المنطقة.
- `extrasTotal()` يجمع `homeExtra` لكل تحليل في السحب المنزلي.
- دفع InstaPay: بيظهر رقم الانستا باي + حقل رفع الإيصال (`setupDropzone`).
- `showSuccess()`: شاشة نجاح فيها كود الحجز + **QR** + أزرار (تتبع، واتساب، طباعة).
- `confetti()`: احتفال بسيط بالكانفس.

### `assets/js/patient.js` — الملف الطبي
دخول بآخر ٦ أرقام من الموبايل → `myBookings()`, `bookingCard()`, `resultCard()`, `drawChart()` (مقارنة النتائج عبر الوقت).

### `assets/js/admin.js` — شاشات الأدمن (١)
`overview` (KPIs + رسوم + **خريطة أوقات الذروة**)، `bookings` (بحث/فلترة/تعديل حالة/QR)، `rx` (تسعير الروشتات)، `patients` (ملف كل مريض).

### `assets/js/admin2.js` — شاشات الأدمن (٢)
`finance` (إيرادات/مصروفات/أرباح)، `tests` و `packages` (CRUD + **استيراد CSV**)، `branches`، `doctors` (**تعديل + حذف نهائي + أسعار كشف منزلي لكل منطقة**)، `areas`، `offers` (كوبونات)، `content` (آراء/FAQ/محتوى)، `msgs` (**مركز رسائل واتساب** بـ 5 قوالب وإرسال جماعي لمرضى اليوم)، `poster` (**ملصق QR للطباعة A4**)، `settings` (بيانات المعمل + النسخ الاحتياطي + **الربط السحابي**)، `logs`.

### `assets/js/doctor.js` — بوابة الطبيب (٩ شاشات)
`home` (حجوزاته)، `mine`، `open` (استلام حجز من المعمل)، `results`، `rx` (الرد على الروشتات)، `patients`، `prices` (**تسعير الكشف المنزلي لكل منطقة**)، الحسابات، الملف الشخصي.

### `assets/js/qr.js` — مولّد QR من الصفر
- جداول GF(256)، ترميز Reed-Solomon بمستوى تصحيح خطأ **M**، الإصدارات 1–6، الأوضاع: بايت (UTF-8) + رقمي.
- 8 أقنعة (masks) مع حساب **penalty** واختيار الأفضل.
- تنسيق معلومات الصيغة (format info) بحسب المواصفة القياسية.
- واجهة: `QR.matrix(text)` / `QR.svg(text,size)` / `QR.canvas()` / `QR.dataURL()`.
- **متحقق منه:** 8/8 فك ترميز بـ jsQR (عربي، روابط طويلة، إصدارات 1–6) + 9/9 تكامل داخل الموقع.

### `assets/js/charts.js` — الرسوم من الصفر (Canvas)
`line()` (خطي متحرك)، `bar()` (أعمدة)، `hbar()` (أفقي)، `donut()` (دائري)، `spark()` (مصغّر)، مع تلميحات (tooltip) وتدرجات لونية وحركة easing.

### `assets/js/sync.js` — المزامنة السحابية (556 سطر)
شوف القسم 6 بالتفصيل.

### `sw.js` + `manifest.json` — PWA
service worker بيخزّن الملفات الأساسية في الكاش (`cache-first` مع تحديث في الخلفية) فالموقع يفتح بدون إنترنت ويتثبت كأبلكيشن.

---

## 5) نظام التصميم (`assets/css/style.css`)

- **داكن فقط** — `html[data-theme="dark"]` هو الوضع الوحيد، ومفيش زر تبديل.
- متغيرات الألوان في `:root`: `--navy #04101c`, `--teal #14b8c4`, `--cyan #38bdf8`, `--gold #fbbf24`, `--card #0c1a29`, `--line #1b2f43`.
- تدرجات: `--grad` (تيل/سماوي)، `--grad-gold`، `--grad-dark`.
- مكوّنات: `.card`, `.svc`, `.test-card`, `.pill`, `.badge`, `.btn` (primary/outline/ghost/gold/danger)، `.stepper/.wizard-panel` (الخطوات)، `.dash/.sidebar/.kpis/.chart-card` (لوحة التحكم)، `.cloud-badge` (شارة المزامنة).
- حركات: `float, pulse, ripple, fadeUp, slideDown, toastIn, spin, breathe, cpulse, ticker`.
- استجابة: `@media (max-width: 992px / 768px / 480px)`.
- `@media print` يخفي الهيدر والفوتر والأزرار ويطبع الإيصال/الملصق بشكل A4 نظيف.
- **مفيش أي إيموجي** — كل الأيقونات SVG (`data-icon` → `hydrateIcons`).

---

## 6) بروتوكول المزامنة (sync.js) بالتفصيل

### المزوّد: Supabase
**المزوّد المستخدم: Supabase** — جدول واحد اسمه **`lab_data`**:

```sql
create table public.lab_data (
  tbl  text,        -- اسم الجدول المنطقي (bookings, tests, ...)
  id   text,        -- معرّف السجل
  data jsonb,       -- السجل كاملاً JSON
  _u   bigint,      -- وقت آخر تعديل (epoch ms)
  _del boolean,     -- علامة حذف
  primary key (tbl, id)
);
```

> الكود فيه كمان محوّل (adapter) جاهز لـ Firebase/Firestore لو احتجته يوم — بيشتغل بنفس البروتوكول، والمزوّد الافتراضي في `sync.js` هو `supabase`.

### تدفق العمل
1. **أول مرة (`boot`)**: `pull()` كامل.
   - لو السحاب **فاضي** → ما يتملّيش `synced` → أول `push()` يرفع بيانات المدير كلها (التحاليل والأسعار والدكاترة).
   - لو السحاب **فيه بيانات** → تُدمج محلياً، وتُعبّأ `synced` بهاش كل سجل (علشان زائر جديد يرفعش البذرة ويمسح بيانات المعمل).
2. **أي تعديل محلي**: `LAB.save()` مُغلّف → بيستدعي `schedulePush()` (بعد 1.2 ثانية) → `diff()` يحسب:
   - سجلات جديدة/متعدلة: `hash(JSON) !== synced`
   - سجلات محذوفة: موجودة في `synced` ومش موجودة محلياً → تُرفع كـ **tombstone** (`_del:true`).
3. **السحب الدوري**: كل 45 ثانية + عند الرجوع للتبويب + عند رجوع الإنترنت.
   - `GET /rest/v1/lab_data?select=...&_u=gt.{ts}` — **سحب تدريجي**: بيجيب السجلات اللي اتعدلت بس.
4. **حل التعارض:** آخر تعديل يكسب (wall-clock). السجل المتعدّل محلياً مش بيتكتب فوقه من السحاب (بيترفع بدل كده). العدّادات `counters` بتاخد **الأكبر** بين المحلي والسحاب (علشان أكواد الحجز متتكررش).
5. **الصور:** أي ملف أكبر من 350KB بيروح لـ Supabase Storage ويُحفظ رابطه بدل الـ base64:
   - `POST /storage/v1/object/lab-files/uploads/...` → رابط عام `.../object/public/lab-files/...`
6. **الفشل/عدم الاتصال:** `setStatus('err')` والموقع يكمل محلي 100% — مفيش أي استثناء بيوصل للمستخدم.
7. **الحالة:** شارة أسفل الشاشة (متصل / جاري المزامنة / تعذر الاتصال / بدون مزامنة).

### أمان القفل (تم إصلاحه)
لو أول سحب فشل، `_push` معموله retry **مرة واحدة بس** — مفيش تكرار لا نهائي.

---

## 7) الأمان والصلاحيات

| الدور | الدخول | الصلاحيات |
|---|---|---|
| المدير (`owner`) | `admin` / `Mo@010` | كل حاجة: كل الشاشات، كل الـ CRUD، حذف الدكاترة، الإعدادات، الربط السحابي |
| طبيب | `heba` / `123456` — `kareem` / `123456` | حجوزاته، تسعير الكشف المنزلي الخاص بيه، الروشتات، مرضاه |
| مريض | الموبايل / آخر ٦ أرقام | ملفه الطبي ونتائجه وحجوزاته فقط |

- **المدير يقدر يحذف أي دكتور نهائياً** ما عدا الحساب اللي داخل بيه حالياً (حماية من قفل نفسه بره).
- البيانات الحساسة (كلمات المرور) موجودة في قاعدة البيانات — لو حابب تقفل أكتر شوف ملاحظات الأمان في آخر ملف `supabase/schema.sql`.

---

## 8) الاختبارات الآلية (نتائج آخر تشغيل)

الاختبارات في مجلد منفصل (`~/.qa`) وتعمل على الموقع عبر jsdom:

| الاختبار | عدد الفحوصات | النتيجة |
|---|---|---|
| `smoke.js` — كل الصفحات تفتح بدون أخطاء JS | 9 صفحات | ✅ 0 أخطاء |
| `views.js` — كل شاشات الأدمن | 16 | ✅ 16/16 |
| `verify.js` — البيانات الحقيقية والداكن والإيموجي والصلاحيات | 18 | ✅ 18/18 |
| `e2e.js` — رحلة حجز كاملة + روشتة + مريض + أدمن + طبيب | 46 | ✅ 46/46 |
| `cloud.js` — مزامنة Supabase (سيرفر وهمي) | 12 | ✅ 12/12 |
| `uicloud.js` — كارت الربط السحابي في لوحة المدير | 8 | ✅ 8/8 |
| `qrtest.js` — فك ترميز QR بـ jsQR | 8 | ✅ 8/8 |
| `qrint.js` — تكامل QR داخل الصفحات | 9 | ✅ 9/9 |
| `nostorage.js` — المتصفح مانع التخزين | 4 | ✅ 4/4 |

**الإجمالي: 121 فحص — 0 فشل.**

---

## 9) أمثلة تعديل سريعة

| عايز تعمل إيه | فين |
|---|---|
| تغيير رقم التليفون أو الانستا باي | لوحة المدير ▸ الإعدادات (أو `core.js` في `settings`) |
| إضافة تحليل أو تعديل سعر | لوحة المدير ▸ التحاليل (أو استيراد CSV) |
| تغيير رسوم السحب لمنطقة | لوحة المدير ▸ المناطق — أو من الطبيب في «أسعار الكشف المنزلي» |
| تغيير باسورد المدير | لوحة المدير ▸ الدكاترة ▸ تعديل |
| تغيير نص الترحيب في الرئيسية | لوحة المدير ▸ المحتوى |
| إضافة طبيب جديد | لوحة المدير ▸ الدكاترة ▸ إضافة (يحدد يوزر وباسورد) |
| طباعة ملصق QR للمعمل | لوحة المدير ▸ ملصق QR ▸ طباعة |
| إرسال رسالة واتساب لمرضى النهاردة | لوحة المدير ▸ مركز الرسائل |

---

## 10) النشر (ملخص)

اتبع **`دليل-النشر-خطوة-بخطوة.md`**:
1. Supabase ← جهّز القاعدة (`schema.sql`) + مجلد الصور + المفاتيح.
2. GitHub Desktop ← Commit + Publish.
3. Vercel ← Import + Deploy.
4. لوحة المدير ▸ الإعدادات ▸ الربط السحابي ← الصق المفاتيح ← اختبار الاتصال.
5. بعد كده أي تعديل: Commit → Push → بينشر أوتوماتيك.

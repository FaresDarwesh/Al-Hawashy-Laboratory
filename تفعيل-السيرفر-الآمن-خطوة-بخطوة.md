# تفعيل السيرفر الآمن — Environment Variables على Vercel
### (خطوة بخطوة بالتفصيل — كل إعداد وله صورة بالكلام)

> **إحنا فين دلوقتي؟**
> الكود كله اترفع ✅ و`/api` شغال ✅ — لكن لما بندّي عليه طلب دلوقتي بيردّ:
> `{"error":"السيرفر غير مضبوط: SUPABASE_URL / SUPABASE_SERVICE_KEY"}`
> وده **طبيعي جدًا** ومعناه إنه ناقص بس المتغيرات اللي في الملف ده. خلصها وهتسمع `{"ok":true...}`.

---

## ⚠️ قاعدتين قبل أي حاجة

1. **المفتاح السرّي (`sb_secret_…`) يتحط في Vercel فقط** — متحطّوش في أي ملف في الكود، ومتبعتوش لحد، ومتكتبوش في `supabase-config.js`.
2. **متستخدمش المفتاح `sb_publishable_…` هنا** — ده مفتاح القراءة العام. إحنا عايزين **Secret key**.

---

## الخطوة 1 — جهّز قاعدة البيانات (مرة واحدة بس)

1. افتح https://supabase.com/dashboard/project/psvcoytngyaklaypagem
2. من القائمة اليسار: **SQL Editor**
3. اضغط **+ New query**
4. افتح ملف `supabase/schema.sql` من الكود اللي فكيت ضغطه → **انسخ محتواه كله** → الصقه في المحرر
5. اضغط **Run** (أو Ctrl + Enter)
6. لازم يظهر **Success. No rows returned**

الملف ده بينشئ جدول `lab_data` **مقفول افتراضيًا** — يعني محدش يقدر يقرا أو يكتب غير عن طريق السيرفر بتاعنا.

---

## الخطوة 2 — هات القيم التلاتة

### ① `SUPABASE_URL`
انسخه زي ما هو:
```
https://psvcoytngyaklaypagem.supabase.co
```

### ② `SUPABASE_SERVICE_KEY`
1. من نفس الصفحة في Supabase: **Project Settings** (أيقونة الترس ⚙️ تحت على اليسار)
2. → **API Keys** (أو **Configuration → API** في النسخة القديمة)
3. هتلاقي قسم **Secret keys** → اضغط **Reveal** جنب المفتاح
4. انسخ المفتاح اللي بيبدأ بـ **`sb_secret_`**
   - لو ملقيتش غير **service_role** (القديم) → خده، شغال بنفس الطريقة
   - لو لقيت **Publishable key** (`sb_publishable_`) → **مش ده** ❌

### ③ `APP_SECRET`
أي كلمة سر طويلة وعشوائية — انسخ دي زي ما هي:
```
Hawashy#BvLVFDLD8VCrtFt-2026
```
(دي بتوقّع بيها توكنات الدخول. لو غيرتها بعدين، كل المستخدمين هيسجلوا خروج تلقائي.)

---

## الخطوة 3 — حطّهم في Vercel

1. افتح https://vercel.com/dashboard
2. افتح مشروع **`al-hawashy-laboratory`**
3. من فوق: **Settings**
4. من القائمة اليسار: **Environment Variables**
5. لكل واحد من التلاتة، املأ كالآتي:

| الحقل | تكتب إيه |
|---|---|
| **Key** | `SUPABASE_URL` |
| **Value** | `https://psvcoytngyaklaypagem.supabase.co` |
| **Environments** | علّم على **Production** + **Preview** + **Development** (التلاتة) |

6. اضغط **Save**
7. كرّر نفس الحاجة للاتنين الباقيين:

| Key | Value |
|---|---|
| `SUPABASE_SERVICE_KEY` | المفتاح `sb_secret_…` اللي نسخته |
| `APP_SECRET` | `Hawashy#BvLVFDLD8VCrtFt-2026` |

> **ملحوظة:** مفيش مسافات في أول أو آخر القيمة، والأسامي **حساسة لحالة الحروف** (كلها كبيرة زي ما هي مكتوبة).

---

## الخطوة 4 — لازم تعمل Redeploy

المتغيرات **مش بتشتغل غير بعد deployment جديد**:

1. من فوق: **Deployments**
2. افتح أول deployment في القائمة (الأحدث)
3. اضغط على **⋮** (تلات نقط) على اليمين
4. → **Redeploy**
5. سيب كل حاجة زي ما هي واضغط **Redeploy**
6. استنى لحد ما الحالة تبقى **Ready** (أقل من دقيقة)

---

## الخطوة 5 — فعّل الوضع الآمن من لوحة الأدمن

1. افتح https://al-hawashy-laboratory.vercel.app/admin.html واضغط **Ctrl + F5**
2. دخول الأدمن: المستخدم `admin` وكلمة السر `Mo@010`
3. من القائمة: **الإعدادات**
4. تحت: **الربط السحابي**
5. **طريقة الاتصال** → اختار:
   **عبر السيرفر الآمن /api — الأأمن (مُوصى به)**
6. **المزامنة** → **مفعّلة**
7. اضغط **حفظ واختبار الاتصال**
8. لازم تظهر رسالة: **«نجح الاتصال — السحاب (سيرفر آمن) جاهز…»**
9. **مهم:** **سجّل خروج** وبعدين **ادخل تاني** (علشان تاخد التوكن الموقّع)
10. هتلاقي الشارة تحت على اليسار كتبت **«متصل بالسحاب»** باللون الأخضر

> مش محتاج تكتب Project URL ولا أي مفتاح جوه الموقع خالص — المتصفح مش بيشوفهم أصلًا.

---

## الخطوة 6 — اتأكد إن كله تمام

بعد ما تعمل Redeploy، افتح الرابط ده في المتصفح:

```
https://al-hawashy-laboratory.vercel.app/api/data
```

- لو ظهر **`{"error":"POST فقط"}`** → السيرفر شغال ✅ (الرسالة دي معناها إنه بيستنى طلب POST، وده الصح)
- لو ظهر **The page could not be found** → فولدر `api` لسه موصلش

---

## جدول الأعطال وحلولها

| الرسالة اللي بتظهر | السبب | الحل |
|---|---|---|
| `السيرفر غير مضبوط: SUPABASE_URL / SUPABASE_SERVICE_KEY` | المتغيرات مش موجودة أو فاضية | راجع الخطوة 3 + اعمل Redeploy |
| `relation "lab_data" does not exist` | الـ schema.sql متشغّلش | نفّذ الخطوة 1 |
| `Invalid JWT` أو `invalid signature` | حاطط المفتاح الـ publishable بدل السرّي | بدّله بـ `sb_secret_…` |
| `JWS` / `permission denied for table lab_data` | الـ RLS مقفول والمفتاح مش service | تأكد إنك نفّذت schema.sql الجديد وإن المفتاح secret |
| `غير مصرّح` في لوحة الأدمن | التوكن قديم أو APP_SECRET اتغير | سجّل خروج ودخول تاني |
| الشارة بتقول **«السيرفر /api مش متاح»** | فولدر api موصلش أو العطل مؤقت | الموقع بيرجع محلي أوتوماتيك وبيكمل شغل عادي |

---

## أسئلة سريعة

**هل لازم أعمل كل ده؟**
لأ. الموقع بيشتغل دلوقتي 100% محلي على كل جهاز. الكلام ده كله عشان **الحجوزات والنتائج تبان على كل الأجهزة وتفضل محفوظة**.

**إيه الفرق بين الوضعين؟**

| | عبر السيرفر الآمن /api | مباشر من المتصفح |
|---|---|---|
| المفتاح في المتصفح؟ | ❌ لأ | ✅ آه (بيبان لأي زائر) |
| مريض يشوف بيانات غيره؟ | ❌ مستحيل | ⚠️ ممكن لو RLS مفتوح |
| محتاج إعداد | 3 متغيرات على Vercel | URL + مفتاح جوه اللوحة |

**الأمان:** النسخة دي مقفولة افتراضيًا — المريض يشوف بياناته بس، والدكتور ممنوع من المصروفات والحسابات، والزائر يشوف التحاليل والباقات والفروع فقط. التفاصيل في `الأمان-والخصوصية.md`.
